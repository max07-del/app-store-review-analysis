# Optional: enables the Claude insight layer.
# Without it the API still returns the complete rule-based analysis.
ANTHROPIC_API_KEY=

# Optional overrides — defaults shown.
# APP_LLM_MODEL=claude-opus-5
# APP_LLM_MAX_REVIEWS=120
# APP_LLM_EFFORT=medium
# APP_LLM_TIMEOUT_SECONDS=120
# APP_DATABASE_PATH=data/app_store_reviews.db
